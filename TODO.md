# Network Mismatch Detection Implementation

## Steps
- [ ] 1. Fix `DepositForm.tsx` (clean up corrupted code + add `isNetworkMismatch` prop)
- [ ] 2. Fix `WithdrawForm.tsx` (clean up corrupted code + add `isNetworkMismatch` prop)
- [ ] 3. Update `WalletContext.tsx` (add `isNetworkMismatch` to context type and value)
- [ ] 4. Create `NetworkMismatchBanner.tsx`
- [ ] 5. Update `dashboard.tsx` (render banner, pass `isNetworkMismatch` to forms)
- [ ] 6. Update `DepositForm.test.tsx` (add network mismatch test)
- [ ] 7. Update `WithdrawForm.test.tsx` (add network mismatch test)
- [ ] 8. Update `useWallet.test.ts` (add network mismatch exposure test)
- [ ] 9. Run tests and build to verify

